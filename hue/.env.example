# Hue Bridge local IP
HUE_BRIDGE_IP=<HUE_BRIDGE_IP>

# Hue API username (generated from the bridge button + API call)
HUE_USERNAME=change-me

# App port (container listens on PORT; compose maps host port separately)
PORT=8000
HUE_HOST_PORT=8000
